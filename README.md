# Build the container

```
sudo sh build.sh
```

# Run

* add the xhprofile file in the folder src/htdocs/runs

```
sudo sh run.sh
```

# Enter the container

```
sudo rkt enter <container uuid> /bin/sh
```

